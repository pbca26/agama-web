// obsolete(?)
let Config = {
  version: '0.1.0',
  debug: true,
  defaultLang: 'EN',
  roundValues: false,
  // mock
  mock: {
    api: {
      /*auth: { status: 'unlocked' },
      allcoins: { spv: ['KMD'], total: 1 },
      coins: { result: ['KMD'] },*/
    }
  }
};

export default Config;